# Game Art & Audio Provenance

- 날짜: 2026-07-26
- 게임: `stormpost`

## 게임 아트

- 시각 매체: Canvas 2D 절차적 레이어드 일러스트와 CSS 타이틀 디오라마
- 원본: 외부 이미지·모델·셰이더를 사용하지 않았다. 저장소에 포함된 Galmuri 글꼴은 `OFL-GALMURI.md`의 라이선스를 따른다.
- 생성·제작 방식: 제작자가 TypeScript Canvas 도형(베지어 돛, 타원형 구름·섬, 선형 비, 호 형태 상승 기류)과 CSS 도형으로 직접 구현했다.
- 후가공: 투명도 레이어, 선형 그라디언트, 화면 섬광, 사인파 부유 애니메이션을 런타임에서 합성한다.
- 사용 위치: 타이틀, 항로, HUD, 계약·결과 화면 전체.
- 절차 변형: 시드로 섬·상승 기류·위험 위치를 정하고, 누적 우표 4/9/15에서 선체 색·돛 색·금빛 돌풍 고리를 코드 도형으로 바꾼다.
- 상태 표현: HUD의 청록 ALT 막대가 고도를 표시하며 25 미만에서 산호색 경고로 바뀐다.
- 안전 통로 표현은 별도 장식선을 추가하지 않고 상승 기류의 청록 동심호를 항로 중심 표식으로 사용한다. 실제 판정은 위험 도형 반경과 선체 충돌 반경까지 포함한다.

## 게임 사운드

- 원본: 외부 오디오 자산을 게임 플레이에서 사용하지 않는다.
- 합성·편집 방식: 첫 포인터·키 입력 뒤 WebAudio AudioContext를 생성한다. 순환 노이즈를 저역 통과시킨 바람층, 좌우 StereoPanner, 사인·삼각·톱니 오실레이터의 짧은 음형을 런타임에서 합성한다.
- 사용 위치: 비행 속도·좌우 위치에 반응하는 바람 드론, 상승 기류의 돛 장력음, 돌풍 상승음, 위험 충돌 저음, 배달 3화음과 관문 개방 화음.
- 수명주기: 첫 입력 전 무음, `M` 음소거, `P`/문서 비활성화/결과 화면에서 suspend, 재시작에서 resume, 게임 destroy에서 source 정지와 AudioContext close.

## 확인 범위

- 게임 런타임에서 네트워크 이미지·오디오 요청이 없음을 소스와 빌드 자산 목록에서 확인했다. 합성음 이벤트와 수명주기는 브라우저 스모크의 콘솔·페이지 오류 0으로 확인했고, 실제 음량·공간감은 기기 청취가 남았다.
- `public/audio/*.mp3`는 셸 템플릿 잔존 자산이며 기존 BGM 셸에서 참조될 수 있다. 게임 고유 제작 자산으로 주장하지 않는다.

## 공개 제작자 일러스트

- 게임 잠금 뒤 `laika-base-v1`을 직접 참조해 라이카가 비행 우편선의 기수를 잡고 봉인 편지를 놓아주는 장면을 OpenAI 내장 `image_gen`으로 생성했다.
- 원본은 `art/source/laika-stormpost.png`, 재현용 지시는 `art/prompts/laika-stormpost.md`, 해시와 검수 결과는 `art/provenance/laika-stormpost.json`에 기록했다.
- 웹 릴리스에는 640px와 1280px JPEG 파생본만 사용한다. 얼굴 무늬, 귀, 하네스, 주황 연결구, 자연스러운 앞발, 생성 문자 부재와 모바일 크롭을 확인했다.
